# Core HTTP + config
requests>=2.32.0
python-dotenv>=1.0.1
PyYAML>=6.0.2

# Video assembly / captions / overlays (optional but used by build_video.py)
# ffmpeg-python wraps the system ffmpeg binary (install ffmpeg separately).
ffmpeg-python>=0.2.0

# Optional: official Higgsfield Python SDK (if you use the SDK path)
# higgsfield-client  # uncomment once you confirm the package name on PyPI

# Tracking report
tabulate>=0.9.0
